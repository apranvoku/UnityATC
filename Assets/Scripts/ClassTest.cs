using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static Flight;

public class ClassTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var flight = new Flight(0, 0, 0, 0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
